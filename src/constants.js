const OPERATORS = {
  add: '+',
  subtract: '-',
  divide: '/',
  multiply: '*',
};

const CLEAR = 'c';
const EQUALS = '=';
const NEGATIVE = '!';

module.exports = {
  CLEAR,
  EQUALS,
  NEGATIVE,
  OPERATORS,
};
